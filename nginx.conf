# OBSOLETO — No necesario con Dokploy.
#
# Dokploy gestiona Traefik automaticamente via la UI (pestaña Domains).
# No se necesita configuracion manual de nginx ni Traefik.
#
# El despliegue anterior en Windows usaba nginx en /v2/.
# El nuevo despliegue con Dokploy sirve en la raiz /.
